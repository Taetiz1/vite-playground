import { FC } from 'react';
declare type CaptureSettingsType = {
    quality?: number;
    type?: string;
};
export declare type CaptureType = {
    trigger: boolean;
    callBack: (capture?: string) => void;
    settings?: CaptureSettingsType;
};
declare type CaptureProps = CaptureType;
declare const Capture: FC<CaptureProps>;
export default Capture;
