import { type ILogger } from './logger';
export interface CallSecret {
    expiresAt: Date;
    hashedData: string;
}
export declare namespace CallSecret {
    interface Flatten {
        expiresAt: string;
        hashedData: string;
    }
    function fromObject(value: object | null, logger: ILogger): CallSecret;
    function checkCallSecret(callSecret: CallSecret, callSecretKey: string, logger: ILogger): void;
}
